using System.Linq;
using UnityEngine;

public class ZombieHands : Knife
{
    
    
    //[PunRPC]
    //public void Sniff(int playerId)
    //{

    //    var player = ToObject<Player>(playerId);
    //    if (player.observing)
    //        _Hud.CenterText("Zombie spotted you", 1);

    //    if (pl.observing)
    //        _ObsCamera.customCamera = new GameObject("Custom camera").Component<CustomCamera>(a => a.target = player.Cam.transform);
    //}
}